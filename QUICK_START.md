# Быстрый старт: Группы сервисов

## Что изменилось?

Добавлена новая система управления сервисами с трёхуровневой структурой:

```
Группы сервисов
  └── Тарифы
       └── Характеристики (динамические поля)
```

Теперь вместо простого списка VPS планов, вы можете создавать разные группы сервисов (MC Video, SSL, VPS и т.д.), внутри каждой группы - тарифы, а у каждого тарифа - свои характеристики.

## Установка (для новых пользователей)

### Шаг 1: Создание таблиц в БД

```bash
cd backend
node scripts/setup-service-groups.js
```

Этот скрипт создаст:
- 3 таблицы (service_groups, service_plans, plan_fields)
- 3 примера групп (MC Video, SSL Сертификаты, VPS)
- Несколько примеров тарифов с характеристиками

### Шаг 2: Запуск

Бэкенд и фронтенд уже готовы к работе. Просто запустите:

```bash
# Backend
cd backend
npm start

# Frontend (в другом терминале)
npm start
```

### Шаг 3: Проверка

1. Войдите как администратор
2. В дашборде откройте вкладку **"Сервисы"**
3. Вы увидите 3 созданные группы с примерами

## Использование

### Создание новой группы сервисов

1. **Откройте** "Сервисы" в дашборде админа
2. **Нажмите** на кнопку "+" в правом верхнем углу
3. **Заполните**:
   - Название на русском и узбекском (обязательно)
   - Slug сгенерируется автоматически
   - Описание (опционально)
   - Иконку (эмодзи или URL)
   - Порядок отображения (меньшее число = выше в списке)
4. **Сохраните**

### Создание тарифа внутри группы

1. **Откройте** группу сервисов
2. **Нажмите** "Тарифы"
3. **Нажмите** "+" для создания нового тарифа
4. **Заполните основную информацию**:
   - Название на двух языках
   - Цена (обязательно)
   - Цена со скидкой (опционально)
   - Период оплаты (месяц/год/единоразово)
   - Порядок отображения
5. **Добавьте характеристики** (нажмите "Добавить поле"):
   - Ключ (латиница, например: cpu, ram, disk)
   - Название на двух языках (например: "Процессор" / "Protsessor")
   - Значение на двух языках (например: "Intel Xeon Gold")
6. **Сохраните**

### Редактирование

- **Группу**: нажмите иконку карандаша на карточке группы
- **Тариф**: откройте список тарифов группы, нажмите иконку карандаша

### Удаление

- Нажмите иконку корзины
- Если есть связанные данные (тарифы в группе или заказы у тарифа), объект будет деактивирован вместо удаления

### Изменение порядка

Измените значение поля "Порядок отображения" при редактировании. Меньшее число означает выше в списке.

## Примеры

### Пример: Группа MC Video

**Группа:**
- Название: "MC Video"
- Slug: "mc-video"
- Иконка: "📹"

**Тариф: NVR-D-720-30**
- Цена: 60 000 UZS
- Скидка: 49 000 UZS
- Период: месяц

**Характеристики:**
| Ключ | Название (RU) | Название (UZ) | Значение (RU) | Значение (UZ) |
|------|---------------|---------------|---------------|---------------|
| resolution | Разрешение | Ruxsatnoma | 720P | 720P |
| bandwidth | Ширина канала | Kanal kengligi | 1 Mbit/s | 1 Mbit/s |
| storage_type | Хранение видеоархива | Videoarxiv saqlash | Запись по детекции | Yozuv detektsiyasi bo'yicha |
| retention | Хранение записей | Kameralar yozuvlarini saqlash | 30 дней | 30 kun |

### Пример: Группа VPS

**Тариф: VPS-25**
- Цена: 142 500 UZS
- Период: месяц

**Характеристики:**
| Ключ | Название | Значение |
|------|----------|----------|
| cpu | Процессор / Protsessor | Intel Xeon Gold CPU - 3,5GHz (1-vCore) |
| ram | Память RAM / Xotira | 1 GB |
| disk | Диск SSD / Disk | 25 GB |
| internet | Интернет / Internet | 100 Mb/s |
| tas_ix | TAS-IX | 100 Mb/s |
| ip | IP адреса / IP Manzil | 1 |
| os | ОС / Operatsion tizim | 🐧 Linux / 💻 Windows |

## Что НЕ изменилось?

- Существующая функциональность VPS планов работает как раньше
- Все существующие данные сохранены
- API endpoints для заказов, пользователей и платежей не изменились
- Структура проекта осталась прежней

## Структура файлов

### Backend (новые файлы):
```
backend/
├── api/services/
│   ├── service-groups-admin.js   # API для групп
│   └── service-plans-admin.js    # API для тарифов
└── scripts/
    └── setup-service-groups.js   # Скрипт создания таблиц
```

### Frontend (новые файлы):
```
app/(admin)/
├── service-groups.tsx           # Список групп
├── service-group-form.tsx       # Форма группы
├── service-plans.tsx            # Список тарифов
└── service-plan-form.tsx        # Форма тарифа
```

## API Endpoints

### Группы сервисов:
- `GET /api/service-groups-admin` - список групп
- `GET /api/service-groups-admin/:id` - одна группа
- `POST /api/service-groups-admin` - создать группу
- `PUT /api/service-groups-admin/:id` - обновить группу
- `DELETE /api/service-groups-admin/:id` - удалить группу
- `PUT /api/service-groups-admin/:id/reorder` - изменить порядок

### Тарифы:
- `GET /api/service-plans-admin?group_id=X` - список тарифов группы
- `GET /api/service-plans-admin/:id` - один тариф с характеристиками
- `POST /api/service-plans-admin` - создать тариф
- `PUT /api/service-plans-admin/:id` - обновить тариф
- `DELETE /api/service-plans-admin/:id` - удалить тариф
- `PUT /api/service-plans-admin/:id/reorder` - изменить порядок

Все endpoints требуют авторизации и роль admin.

## Часто задаваемые вопросы

**Q: Можно ли использовать и старые VPS планы, и новые группы сервисов?**
A: Да, они работают параллельно. Старая система VPS планов не затронута.

**Q: Как удалить примеры данных?**
A: Просто удалите группы через админ-панель, используя иконку корзины.

**Q: Можно ли добавить больше языков?**
A: Да, но потребуется изменить структуру БД и формы. Сейчас поддерживаются русский и узбекский.

**Q: Что означает "billing_period"?**
A: Это период оплаты тарифа:
- `monthly` - ежемесячно
- `yearly` - ежегодно
- `once` - единоразовая оплата

**Q: Как работает порядок отображения?**
A: Элементы сортируются по полю `display_order` по возрастанию. Например, order=1 будет выше, чем order=5.

**Q: Что такое "slug"?**
A: Это уникальный идентификатор для URL, например: "mc-video", "ssl-certificates". Используется в адресной строке.

## Поддержка

Если что-то не работает:

1. **Проверьте логи backend** в консоли
2. **Убедитесь, что таблицы созданы** (запустите `setup-service-groups.js`)
3. **Проверьте права доступа** - только администраторы видят эту функциональность
4. **Очистите кэш** браузера или приложения

## Дополнительная информация

Подробная документация: `SERVICE_GROUPS_FEATURE.md`

---

**Готово!** Теперь вы можете управлять группами сервисов и тарифами через удобный интерфейс админ-панели.
